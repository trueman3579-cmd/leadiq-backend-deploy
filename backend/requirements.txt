fastapi>=0.115.0
uvicorn[standard]>=0.30.0
redis>=5.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
httpx>=0.27.0
feedparser>=6.0.11
celery>=5.4.0
python-telegram-bot>=21.0.0    # Outbound notifications (bot API)

# ── Telegram Inbound Scraper ────────────────────────────────────────────────
telethon>=1.26.0                # Inbound message scraping (MTProto API)
praw>=7.8.0

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Structured Logging + Sentry ───────────────────────────────────────────────
structlog>=24.0.0
sentry-sdk[fastapi]>=2.0.0

# ── MCP (Model Context Protocol) ─────────────────────────────────────────
mcp[cli]>=1.9.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
# Google Gemini via direct API key (Google AI Studio) OR via Vertex AI (GCP)
# For Vertex AI auth (GCP service account / Application Default Credentials)
# LangExtract - Google's library for structured extraction with source grounding
langextract>=0.1.0

# ── Enrichment APIs ────────────────────────────────────────────────────────────
# Hunter.io email finder integration
aiohttp>=3.9.0
# Clearbit company enrichment integration
aiohttp>=3.9.0

# ── Vector Search ──────────────────────────────────────────────────────────────
# pgvector for semantic similarity and deduplication
pgvector>=0.2.0

# ── Test dependencies ─────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
slowapi>=0.1.9
